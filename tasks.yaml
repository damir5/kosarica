tasks:
  # ============================================================================
  # TypeScript (Drizzle) Migrations
  # ============================================================================

  - title: "Create PL/pgSQL function: get_pending_matches()"
    description: |
      In `drizzle/0001_manual_functions.sql`, add:
      ```sql
      CREATE OR REPLACE FUNCTION get_pending_matches(
        p_status_filter text[],
        p_chain_filter text[],
        p_limit integer DEFAULT 100
      ) RETURNS TABLE (
        -- TODO: define return columns based on query in src/orpc/router/products.ts:97-159
      ) AS $$
        -- CTE with JSON aggregation from lines 97-159
      $$ LANGUAGE plpgsql STABLE;
      ```

      Migrate `src/orpc/router/products.ts` lines 97-159 (`getPendingMatches` function)
    completed: false

  - title: "Create PL/pgSQL function: bulk_approve_matches()"
    description: |
      In `drizzle/0001_manual_functions.sql`, add:
      ```sql
      CREATE OR REPLACE FUNCTION bulk_approve_matches(
        p_queue_ids text[],
        p_user_id text
      ) RETURNS TABLE (
        -- TODO: define return columns
      ) AS $$
        -- CTE with INSERT/UPDATE/ON CONFLICT from lines 367-403
      $$ LANGUAGE plpgsql;
      ```

      Migrate `src/orpc/router/products.ts` lines 367-403 (`bulkApprove` function)
    completed: false

  - title: "Migrate searchProducts pg_trgm to Drizzle"
    description: |
      `src/orpc/router/products.ts` lines 480-494 (`searchProducts` function)
      - Current: pg_trgm similarity raw SQL
      - Migration: Drizzle with `sql` fragment
    completed: false

  - title: "Migrate getStats aggregations to Drizzle"
    description: |
      `src/orpc/router/products.ts` lines 512-545 (`getStats` function)
      - Current: 3x aggregate queries
      - Migration: Drizzle aggregations
    completed: false

  - title: "Migrate scheduleTask INSERT to Drizzle"
    description: |
      `src/lib/taskqueue/index.ts` lines 30-36 (`scheduleTask` function)
      - Current: INSERT with RETURNING raw SQL
      - Migration: Drizzle `.insert().returning()`
    completed: false

  - title: "Migrate startProcessing UPDATE to Drizzle"
    description: |
      `src/lib/taskqueue/index.ts` lines 64-68 (`startProcessing` function)
      - Current: UPDATE statement raw SQL
      - Migration: Drizzle `.update().set()`
    completed: false

  - title: "Migrate getTask SELECT to Drizzle"
    description: |
      `src/lib/taskqueue/index.ts` lines 85-92 (`getTask` function)
      - Current: SELECT statement raw SQL
      - Migration: Drizzle `.select().where()`
    completed: false

  - title: "Create PL/pgSQL function: claim_due_cron_jobs()"
    description: |
      In `drizzle/0001_manual_functions.sql`, add:
      ```sql
      CREATE OR REPLACE FUNCTION claim_due_cron_jobs(
        p_cutoff timestamptz
      ) RETURNS TABLE (
        -- TODO: define return columns
      ) AS $$
        -- UPDATE with FOR UPDATE SKIP LOCKED from lines 46-70
      $$ LANGUAGE plpgsql;
      ```

      Migrate `src/jobs/cron/executor.ts` lines 46-70 (`claimDueJobs` function)
      - Exception: Drizzle doesn't support SKIP LOCKED, hence PL/pgSQL function
    completed: false

  # ============================================================================
  # Go sqlc Query Files to Create
  # ============================================================================

  - title: "Create internal/database/queries/matching.sql"
    description: |
      Barcode matching queries for `services/price-service/internal/matching/barcode.go`:
      - Lines 42-62: `AutoMatchByBarcode` - Streaming SELECT
      - Lines 149-155: barcode mapping INSERT
      - Lines 168-176: product links INSERT with ON CONFLICT
      - Lines 281-285: create product INSERT with RETURNING
      - Lines 298-314: queue for review INSERT with ON CONFLICT

      Note: Lines 117 advisory lock `pg_advisory_xact_lock()` - keep as-is (Postgres-specific)
    completed: false

  - title: "Create internal/database/queries/ai_matching.sql"
    description: |
      AI matching queries for `services/price-service/internal/matching/ai.go`:
      - Lines 276-277: similarity search with pg_trgm
      - Lines 340-345: rejection check EXISTS subquery
      - Lines 390-408: insert candidate INSERT with ON CONFLICT UPDATE
      - Lines 422-426: create link INSERT with ON CONFLICT
    completed: false

  - title: "Create internal/database/queries/cleanup.sql"
    description: |
      Cleanup job queries for `services/price-service/internal/jobs/cleanup_audit.go`:
      - Lines 31-44: DELETE candidates with date filter
      - Lines 72-88: UPDATE queue with status + date
      - Lines 166-195: 3x COUNT queries
    completed: false

  - title: "Create internal/database/queries/embeddings.sql"
    description: |
      Embedding cache queries for `services/price-service/internal/matching/embedding.go`:
      - Lines 63-71: `getCachedEmbeddings` - SELECT with ANY()
      - Lines 120-124: `getCachedProductEmbeddings` - SELECT with ANY()
      - Lines 170-179: `storeEmbeddingCache` - INSERT with ON CONFLICT UPDATE
      - Lines 265-273: `EnsureProductEmbeddings` - SELECT with NOT EXISTS
    completed: false

  - title: "Create internal/database/queries/optimizer.sql"
    description: |
      Price cache queries for `services/price-service/internal/optimizer/cache.go`:
      - Lines 273-278: load stores SELECT with filter
      - Lines 314-318: load price tiers SELECT with filter
      - Lines 358-363: load store refs SELECT with JOIN
      - Lines 612-616: `getActiveChains` - SELECT DISTINCT
    completed: false

  - title: "Create internal/database/queries/taskqueue_extended.sql"
    description: |
      Extended task queue operations for `services/price-service/internal/taskqueue/queue.go`:
      - Lines 155-159: `ScheduleChildTask` - INSERT with RETURNING
      - Lines 171-178: `TransitionToWaiting` - UPDATE statement
      - Lines 201-204: `ScheduleBatchChildTasks` - Batch INSERT
    completed: false

  # ============================================================================
  # Go Handler Migrations to sqlc
  # ============================================================================

  - title: "Migrate barcode.go to use sqlc"
    description: |
      `services/price-service/internal/matching/barcode.go`
      - Use queries from matching.sql
      - Keep lines 117 advisory lock as-is
    completed: false

  - title: "Migrate ai.go to use sqlc"
    description: |
      `services/price-service/internal/matching/ai.go`
      - Use queries from ai_matching.sql
    completed: false

  - title: "Migrate embedding.go to use sqlc"
    description: |
      `services/price-service/internal/matching/embedding.go`
      - Use queries from embeddings.sql
    completed: false

  - title: "Migrate cleanup_audit.go to use sqlc"
    description: |
      `services/price-service/internal/jobs/cleanup_audit.go`
      - Use queries from cleanup.sql
    completed: false

  - title: "Migrate cache.go (optimizer) to use sqlc"
    description: |
      `services/price-service/internal/optimizer/cache.go`
      - Use queries from optimizer.sql
    completed: false

  - title: "Create internal/database/queries/task_finalize.sql"
    description: |
      Task finalize queries for `services/price-service/internal/handlers/task_finalize.go`:
      - Lines 46-49: check failed tasks - SELECT COUNT with LIKE
      - Lines 100-115: `calculateRunStatsFromDB` - 2x COUNT queries
      - Lines 120-130: `updatePriceTierStoreCounts` - UPDATE with subquery
    completed: false

  - title: "Migrate task_finalize.go to use sqlc"
    description: |
      `services/price-service/internal/handlers/task_finalize.go`
      - Use queries from task_finalize.sql
    completed: false

  - title: "Create internal/database/queries/task_discover.sql"
    description: |
      Task discover queries for `services/price-service/internal/handlers/task_discover.go`:
      - Lines 67-70: `recoverRunIDFromDatabase` - SELECT ORDER BY
      - Lines 87-91: `createIngestionRunForDiscover` - INSERT with RETURNING (already sqlc)

      Note: Lines 87-91 already using sqlc, skip if already implemented
    completed: false

  - title: "Migrate task_discover.go to use sqlc"
    description: |
      `services/price-service/internal/handlers/task_discover.go`
      - Use queries from task_discover.sql
    completed: false

  - title: "Add sqlc queries for queue.go child task operations"
    description: |
      `services/price-service/internal/taskqueue/queue.go`
      - Add queries from taskqueue_extended.sql for child task operations

      Note: Keep lines 82-84, 113, 118, 124 as-is (PL/pgSQL function calls that sqlc can't infer)
    completed: false

  # ============================================================================
  # Exceptions (Keep as-is - reference only)
  # ============================================================================

  - title: "Verify exceptions are kept as-is"
    description: |
      Verify the following are NOT migrated (already correct):

      **Existing PL/pgSQL function calls** (sqlc cannot infer complex return types):
      - `src/lib/taskqueue/index.ts` line 50: `claim_tasks()` - Keep as-is
      - `src/lib/taskqueue/index.ts` line 58: `complete_task()` - Keep as-is
      - `src/lib/taskqueue/index.ts` line 77: `fail_task()` - Keep as-is
      - `services/price-service/internal/taskqueue/queue.go` line 82-84: `ClaimTasks` calling `claim_tasks()` - Keep as-is
      - `services/price-service/internal/taskqueue/queue.go` line 113: `CompleteTask` calling `complete_task()` - Keep as-is
      - `services/price-service/internal/taskqueue/queue.go` line 118: `FailTask` calling `fail_task()` - Keep as-is
      - `services/price-service/internal/taskqueue/queue.go` line 124: `CleanupOldTasks` calling `cleanup_old_tasks()` - Keep as-is
      - `services/price-service/internal/sweepers/taskqueue.go` line 69-71: `RecoverOrphanedTasks` calling `recover_orphaned_tasks()` - Keep as-is

      **Advisory locks** (Postgres-specific, no ORM equivalent):
      - `services/price-service/internal/matching/barcode.go` line 117: `pg_advisory_xact_lock()` - Keep as-is
      - `services/price-service/internal/handlers/task_cluster.go` line 1142: `pg_advisory_xact_lock()` - Keep as-is

      **SET LOCAL** (Session configuration):
      - `services/price-service/internal/handlers/task_cluster.go` lines 1131-1139 - Keep as-is

      **Complex temp tables** (Multi-step bulk operations):
      - `services/price-service/internal/handlers/task_cluster.go` lines 928-975 - Keep as-is
      - `services/price-service/internal/handlers/task_cluster.go` lines 1146-1303 - Keep as-is
    completed: false

  # ============================================================================
  # Collaborative Review
  # ============================================================================

  - title: "Collaborative Review: SQL Migration Tracking List"
    description: |
      Multi-model collaborative review with weighted consensus.

      ## Review Process
      Use `/dev:collab reviewer consensus` to review implementation:

      Models (from ~/claude-collab/collaboration.yaml):
      1. github-copilot/gpt-5.2 (weight: 3) - Implementation review
      2. github-copilot/gemini-3-pro-preview (weight: 3) - Architecture review
      3. opencode/grok-code (weight: 1) - Investigation/fact-checking
      4. claude-cli/opus (weight: 4) - Final comprehensive review

      ## Consensus Rules
      - Load weights from ~/claude-collab/collaboration.yaml
      - Model weights: grok=1, gemini-pro=3, gpt-5.2=3, opus=4
      - If models disagree, favor higher-weighted model opinions
      - All critical issues flagged by ANY model must be addressed
      - Document consensus and any dissenting opinions

      ## Review Focus Areas
      - PL/pgSQL function correctness and return types
      - Drizzle query builder patterns
      - sqlc query file syntax and type inference
      - Advisory lock usage is preserved where needed
      - All exceptions are properly documented
      - No raw SQL remains in TypeScript/Go except documented exceptions
    completed: false
